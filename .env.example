DATABASE_URL="file:./data/prod.db"
JWT_SECRET="change-me"
SESSION_SECRET="change-me"
PORT=3000
NODE_ENV=production

# Email (optional). If SMTP_HOST is unset, emails are logged/previewed instead of actually sent.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="BizSuite <no-reply@example.com>"
